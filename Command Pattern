public interface Command{
    public void execute();
}

public class LightOnCommand{
    public Light light;
    
    public LightOnCommand(Light light){
        this.light = light;
    }
    
    public void execute(){
        light.turnOn();
    }
}
